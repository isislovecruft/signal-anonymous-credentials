[package]
name = "wasm"
version = "0.1.0"
authors = ["Isis Lovecruft <isis@patternsinthevoid.net>"]
edition = "2018"

[lib]
name = "credential"
crate-type = ["cdylib"]

[dependencies]
ffi  = { version = "0.1.0", path = "../ffi", features = ["u32_backend"] }
wasm-bindgen = { version = "0.2" }
